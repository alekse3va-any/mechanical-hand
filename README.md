# Управляемая механическая рука

Проект представляет собой механическую руку с веб-интерфейсом управления. Пользователь отправляет команду через сайт, Flask-сервер передаёт её на Arduino по USB, Arduino управляет сервоприводом и возвращает состояние датчика в ладони.

## Идея проекта

Рука имеет четыре пальца. Каждый палец состоит из фаланг и сгибается за счёт нити, работающей как сухожилие. Нити всех пальцев объединены в одну общую тягу. Тяга крепится к катушке сервопривода. При вращении сервопривода тяга натягивается, пальцы сгибаются, и рука переходит в состояние захвата. Разгибание выполняется возвратными пружинами.

В ладони установлена кнопка. Если предмет нажимает на кнопку, система определяет, что объект находится в руке.

## Назначение файлов в директории проекта

| Файл                             | Назначение                                                                                                                                               |
| -------------------------------- | -------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `README.md`                      | Главный файл описания проекта. Содержит общую информацию о механической руке, принципе работы, 3D-модели, состояниях системы, запуске и управлении.      |
| `app.py`                         | Основной серверный файл проекта. Реализует Flask-сервер, принимает команды с веб-интерфейса и передаёт их на Arduino через последовательный порт USB.    |
| `index.html`                     | Главная HTML-страница веб-интерфейса. На ней расположены кнопки управления рукой и блоки отображения текущего состояния руки и наличия объекта.          |
| `app.js`                         | JavaScript-файл для работы веб-интерфейса. Отправляет запросы на Flask-сервер при нажатии кнопок «Сжать руку», «Разжать руку» и «Обновить статус».       |
| `style.css`                      | Файл оформления сайта. Отвечает за внешний вид веб-интерфейса: расположение элементов, цвета, кнопки, карточки состояния и общий стиль страницы.         |
| `mechanical_hand_controller.ino` | Скетч для Arduino. Принимает команды `OPEN`, `CLOSE`, `STATUS`, управляет сервоприводом и считывает состояние кнопки в ладони.                           |
| `requirements.txt`               | Файл со списком Python-зависимостей, необходимых для запуска серверной части проекта. Используется командой `pip install -r requirements.txt`.           |
| `status.json`                    | Файл хранения текущего состояния системы. Содержит данные о состоянии руки и наличии объекта в захвате.                                                  |
| `algorithm.md`                   | Документ с описанием алгоритма работы системы: получение команды, передача её на Arduino, движение сервопривода и получение обратной связи от датчика.   |
| `assembly.md`                    | Инструкция по сборке проекта. Описывает последовательность подготовки механической части, подключения электроники и запуска системы.                     |
| `construction.md`                | Описание конструкции механической руки. Содержит информацию о пальцах, фалангах, нитях, общей тяге, сервоприводе, пружинах и кнопке в ладони.            |
| `electronics.md`                 | Описание электронной части проекта. В файле указано назначение Arduino, сервопривода, кнопки-датчика, питания и соединений между компонентами.           |
| `models.md`                      | Файл с описанием используемых 3D-моделей. Содержит информацию о выбранной основе для печати руки и ссылки на готовые модели.                             |
| `parts_list.md`                  | Список деталей и материалов, необходимых для сборки проекта: Arduino, сервопривод, источник питания, кнопка, нити, пружины, крепёж и 3D-печатные детали. |


## Общая структура проекта

Проект состоит из трёх основных частей:

1. **Веб-интерфейс** — файлы `index.html`, `style.css`, `app.js`.
2. **Серверная часть** — файлы `app.py`, `requirements.txt`, `status.json`.
3. **Аппаратная часть** — файл `mechanical_hand_controller.ino`, документация по электронике, сборке и конструкции.

Веб-интерфейс используется пользователем для отправки команд. Flask-сервер принимает эти команды и передаёт их на Arduino. Arduino управляет сервоприводом механической руки и считывает кнопку в ладони, определяющую наличие объекта.


## 3D-модель

Основой механической части выбрана готовая 3D-печатная модель **InMoov Hand**. Она используется как база для ладони, пальцев, шарниров и сухожильной передачи.

Ссылки на модели:

- https://inmoov.fr/inmoov-hand/
- https://inmoov.fr/hand-and-forarm/
- https://www.thingiverse.com/thing:17773

Модель используется в учебных некоммерческих целях с указанием автора и источника.

## Состояния системы

| Состояние | Описание |
|---|---|
| open | рука разжата |
| closed | рука сжата |
| object: yes | объект обнаружен кнопкой в ладони |
| object: no | объект не обнаружен |


## Запуск

1. Загрузить скетч в Arduino.
2. Подключить сервопривод к пину D9.
3. Подключить кнопку ладони к пину D2 и GND.
4. Подключить внешний источник питания сервопривода.
5. Соединить GND Arduino и GND источника питания сервопривода.
6. Установить зависимости и запустить сервер:

```bash
pip install -r requirements.txt
python app.py
```

После запуска открыть в браузере:

```text
http://127.0.0.1:5000
```

## Управление

На сайте есть три кнопки:

- «Разжать руку»;
- «Сжать руку»;
- «Обновить статус».

Кнопки отправляют запросы на Flask-сервер. Сервер передаёт команды Arduino:

```text
OPEN
CLOSE
STATUS
```

Arduino управляет сервоприводом и возвращает состояние руки и датчика ладони.

